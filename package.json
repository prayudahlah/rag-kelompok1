{
  "name": "rag-kelompok1",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "ingestion",
    "packages/*",
    "server",
    "client",
    "eval"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build:shared": "npm run build -w @rag/shared",
    "build": "npm run build:shared && npm run build -w server && npm run build -w client",
    "typecheck": "npm run typecheck -w ingestion && npm run typecheck -w server && npm run typecheck -w client"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
